<h2><%= @article.title %></h2> <%= link_to "Edit", edit_article_path %> <%= link_to "Delete", article_path(@article), method: :delete, data: { confirm: "Are you sure?" } %>
<p><%= @article.content %></p>

<%= link_to "Go back to all articles", articles_path %>
